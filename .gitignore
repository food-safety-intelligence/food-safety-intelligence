# Data — reproducible from notebooks/scripts, never commit.
# Anchored so the Python subpackage `src/foodsafety/data/` (which contains
# `labels.py`) is NOT silently ignored by an unanchored `data/` pattern.
/data/
# Web-app generated JSONs are gitignored except the small demo fallback —
# loader in scores-server.ts falls back to scores_mock.json when scores.json
# is absent, so a fresh clone can render without running the pipeline first.
# Note: a leaf-level pattern is required here. Git's negation rule does NOT
# re-include files whose parent directory is excluded, so we can't write
# `/app/public/data/` + `!scores_mock.json`.
/app/public/data/*.json
!/app/public/data/scores_mock.json
**/_partial_*/

# Saved model files
*.joblib
*.pkl

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.env

# Jupyter
.ipynb_checkpoints/
*-checkpoint.ipynb

# Tooling
.ruff_cache/
.pytest_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/

# uv
.uv/

# Build artifacts
dist/
build/
*.egg-info/

# Next.js (in app/)
app/node_modules/
app/.next/
app/out/
app/.vercel/
app/next-env.d.ts
app/.env.local
app/.env.development.local
app/.env.production.local

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# JupyterLab autosave/lock artifacts
.~*.ipynb
